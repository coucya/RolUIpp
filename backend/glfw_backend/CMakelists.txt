
aux_source_directory(./ glfw_backend_source)
add_library(RolUI_glfw_backend STATIC ${glfw_backend_source})
target_include_directories(RolUI_glfw_backend PRIVATE ${RolUI_INCLUDE_DIR})
target_include_directories(RolUI_glfw_backend PRIVATE ${3RDPARTY_DIR}/glfw/include)
target_include_directories(RolUI_glfw_backend PRIVATE ${3RDPARTY_DIR}/glad/include)
target_include_directories(RolUI_glfw_backend PRIVATE ${3RDPARTY_DIR}/nanovg/src)
target_include_directories(RolUI_glfw_backend PRIVATE ${3RDPARTY_DIR}/stb)
target_link_libraries(RolUI_glfw_backend glfw glad)
target_link_libraries(RolUI_glfw_backend nanovg)
target_link_libraries(RolUI_glfw_backend RolUI-shared)
target_compile_options(RolUI_glfw_backend PRIVATE --std=c++17)
