
aux_source_directory(./ image_source)
add_executable(image ${image_source})
target_include_directories(image PRIVATE ${RolUI_INCLUDE_DIR})
target_include_directories(image PRIVATE ${BACKEND_DIR})
target_include_directories(image PRIVATE ${3RDPARTY_DIR}/glfw/include)
target_include_directories(image PRIVATE ${3RDPARTY_DIR}/stb)
target_link_libraries(image RolUI_glfw_backend)
target_link_libraries(image RolUI)
target_compile_options(image PRIVATE -std=c++17)
